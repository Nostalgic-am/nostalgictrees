# Access transformers for Nostalgic Trees - 26.1.2
#
# Starting with an empty AT. We'll add entries only when the compiler proves
# they're needed, and only after confirming the target still exists in 26.1.2.
#
# Build.gradle has validateAccessTransformers=false during the port so we can
# compile Java code without getting stuck on AT validation errors. Turn it back
# on at the end to catch typos.
